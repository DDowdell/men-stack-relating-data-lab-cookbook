<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%- include('./partials/_navbar.ejs') %>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
  <title>CookBook</title>
  <style>
      body {
          font-family: 'Arial', sans-serif;
          background-color: #f6f6f6;
          color: #333;
          margin: 0;
          padding: 20px;
          text-align: center;
      }

      h1 {
          font-size: 2.5em;
          color: #e74c3c;
          margin-bottom: 10px;
      }

      h2 {
          font-size: 1.8em;
          color: #f39c12;
          margin: 10px 0;
      }

      .welcome-container {
          background-color: #fff;
          border-radius: 10px;
          box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
          padding: 40px;
          margin: 20px auto;
          max-width: 600px;
      }

      .icon {
          font-size: 100px;
          color: #f39c12;
          margin-bottom: 20px;
      }
  </style>
</head>

<body>
  <div class="welcome-container">
    <i class="fas fa-utensils icon"></i>
    <% if (user) { %>
      <h1>Welcome to the cookbook, Chef <%= user.username %>. Let's get cooking!</h1>
    <% } else { %>
      <h1>Welcome to the cookbook, new chef.</h1>
      <h2>Ready to get cooking?</h2>
    <% } %>
  </div>
</body>

</html>