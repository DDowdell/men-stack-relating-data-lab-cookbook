<nav>
  <ul class="navbar">
    <li><a href="/">Home</a></li>
    <% if (user) { %>
      <li><a href="/users/<%= user._id %>/foods">View My Pantry</a></li>
      <li><a href="/users">Explore Our Community</a></li>
      <li><a href="/auth/sign-out">Sign Out</a></li>
    <% } else { %>
      <li><a href="/auth/sign-in">Sign In</a></li>
      <li><a href="/auth/sign-up">Sign Up</a></li>
    <% } %>
  </ul>
</nav>

<style>
  nav {
      background-color: red;
      padding: 10px 20px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
  }

  .navbar {
      list-style-type: none;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: space-around;
      align-items: center;
  }

  .navbar li {
      margin: 0 15px;
  }

  .navbar a {
      text-decoration: none;
      color: #fff;
      font-weight: bold;
      transition: color 0.3s;
  }

  .navbar a:hover {
      color: #f39c12;
  }

  @media (max-width: 600px) {
      .navbar {
          flex-direction: column;
          align-items: flex-start;
      }

      .navbar li {
          margin: 5px 0;
      }
  }
</style>